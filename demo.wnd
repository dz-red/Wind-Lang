int.t = time.now()
terminal.paste -> "unix-время: {int.t}"
frac.a = time.clock()
int.i = 0
repeat 3000000
    i += 1
end
frac.dt = time.clock() - a
terminal.paste -> "цикл занял {frac.dt} сек"
time.sleep(0,3)
terminal.paste -> "поспал 0.3с"
